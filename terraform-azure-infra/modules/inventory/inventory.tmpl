[vm]
${vm_ip} ansible_user=${vm_user} ansible_ssh_private_key_file=private_key.pem

[acr]
localhost

[all:vars]
acr_name=${acr_name}
acr_username=${acr_username}
acr_password=${acr_password}

[localhost]
127.0.0.1 ansible_connection=local
